[user]
	email = z@alz.ee
	name = Al Zee
	signingKey = less
[alias]
	s = show
	i = init
	st = status
	sw = switch
	p = pull
	l = log --format='%C(auto)%h %Cred%cd%Creset %s' --date=format:'%b %d %H:%M'
	d = diff
	b = branch
[core]
    # In root of working tree, issue
    # mkdir .githooks; ln -s ../.githooks/ .git/
    # Now hooks in .githooks will be tracked and can be triggered whether or not "during a push",
    # as long as it's not a bare repo.
    hooksPath = .githooks/
[commit]
[pull]
	ff = true
