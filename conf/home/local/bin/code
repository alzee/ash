#!/bin/bash
#
# vim:ft=bash

agent="$1"

case "$agent" in
    copilot)
        : 
        ;;
    gemini)
        : 
        ;;
    codex)
        :
        ;;
    cursor)
        agent=cursor-agent
        ;;
    claude|*)
        agent=claude
        ;;
esac

$agent
