#!/bin/bash
#
# vim:ft=bash

# opts=$(getopt -o a: -- "$@")
# eval set -- "$opts"

agent="$1"

case "$agent" in
    copilot)
        : 
        ;;
    gemini)
        : 
        ;;
    codex)
        :
        ;;
    cursor)
        agent=cursor-agent
        ;;
    claude|*)
        agent=claude
        ;;
esac

$agent
