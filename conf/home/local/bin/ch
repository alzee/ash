#!/bin/bash
#
# vim:ft=bash

dir=${1:-z}
prefix=~/.chromium
# prefix=~/.config/google-chrome
chrome=chromium-browser
if ! which $chrome &> /dev/null; then
    chrome=google-chrome
fi

# $chrome --profile-directory=$profile
$chrome --user-data-dir=$prefix/$dir 2> /dev/null &
