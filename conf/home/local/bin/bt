#!/bin/bash
#
# vim:ft=sh

############### Variables ###############

############### Functions ###############

############### Main Part ###############
if ! pidof transmission-daemon > /dev/null; then
    transmission-daemon
    sleep 0.5	# why
fi

if [ "$1" ];then
    transmission-remote "$@"
else
    transmission-remote -l
fi
