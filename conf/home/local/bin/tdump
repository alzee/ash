#!/bin/bash
#
# vim:ft=bash


dir=~/.tcpdump
file=$(date +%Y%m%d-%H%M%S).pcap
[ -d "$dir" ] || mkdir $dir

# tcpdump -U -w - | tee $dir/$file | tcpdump -r - -n -l
tcpdump -w $dir/$file --print

echo Saved file: $dir/$file
